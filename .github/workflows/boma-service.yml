name: Boma service contract

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '17 3 * * 1'

permissions:
  contents: read

jobs:
  quality:
    uses: ./.github/workflows/service-quality.yml
    with:
      service: enkat
      runtime: laravel
      composer-path: '.'
      node-path: '.'
